# Copy to .dev.vars and fill in for local development (.dev.vars is git-ignored).
# The same names must be set as secrets in the Cloudflare Pages dashboard.
WEATHERAPI_KEY=your-weatherapi-key-here
GROQ_API_KEY=your-groq-key-here
